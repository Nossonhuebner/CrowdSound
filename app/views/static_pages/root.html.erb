<% if logged_in? %>
  <script type="text/javascript">
    const currentUserAndTracks = <%= render("api/users/user.json.jbuilder",
    user: current_user ).html_safe %>
    window.currentUser = currentUserAndTracks.user
    window.currentUserTracks = currentUserAndTracks.tracks
  </script>
<% end %>

<main id="root"></main>
